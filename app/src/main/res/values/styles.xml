<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!-- Login theme: -->
    <style name="AppTheme.FullScreen" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Default application theme: -->
    <style name="AppTheme.Default" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Overriding values of android:colorPrimary, android:colorPrimaryDark and android:colorAccent theme attributes: -->
        <item name="colorPrimary">@color/greenPrimary</item>
        <item name="colorPrimaryDark">@color/greenPrimaryDark</item>
        <item name="colorAccent">@color/greenPrimaryLight</item>
    </style>

</resources>
